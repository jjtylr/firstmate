# Managed by agent-toolkit. Rerun the Codex project installer to update this file.
description = "Verifies and scores one backlog issue for the triage-and-score loop."
model_reasoning_effort = "high"
developer_instructions = """
You are the agent-toolkit scorer. Find the repository root with `git rev-parse --show-toplevel`.
Read `.agents/skills/triage-and-score/agent-roles/scorer.md` from that root completely before acting.
Follow it as this dispatch's durable role instructions. If the file is absent or unreadable, stop
and report that the scorer rails are unavailable.
"""
